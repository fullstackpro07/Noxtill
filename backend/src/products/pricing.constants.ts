export const PRICING_ERROR_CODES = {
  NO_MATCHING_PRODUCTS: 'pricing.no_matching_products',
};

/** Target margin a low-margin product's suggested price aims for. */
export const TARGET_MARGIN_RATE = 0.3;
/** A product isn't flagged as underpriced unless its current margin is below this. */
export const LOW_MARGIN_RATE = 0.15;
